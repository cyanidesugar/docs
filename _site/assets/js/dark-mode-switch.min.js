!(function () {
  let t;
  const e = document.getElementById('darkSwitch');
  if (e) {
    const switchStored = localStorage.getItem('darkSwitch');
    if (switchStored !== null && switchStored === 'dark') {
      e.checked = t;
      document.body.setAttribute('data-theme', 'dark');
    } else { document.body.removeAttribute('data-theme'); }
    e.addEventListener('change', () => {
      if (e.checked) {
        document.body.setAttribute('data-theme', 'dark');
        localStorage.setItem('darkSwitch', 'dark');

      } else {
        document.body.removeAttribute('data-theme');
        localStorage.removeItem('darkSwitch');
      }
    });
  }
}());

/*
document.getElementById('darkSwitch').addEventListener('click',()=>{
    if (document.body.classList.contains('dark')) {
        document.body.classList.remove('dark')
    }
    else {
        document.body.classList.add('dark')
    }
   // document.getElementById('darkTheme').innerText = document.body.classList
})*/

/*
!(function () {
  let t;
  if (e) {
    const switch = localStorage.getItem('darkSwitch')
    t = localStorage.getItem('darkSwitch') !== null && localStorage.getItem('darkSwitch') === 'dark', (e.checked = t) ? document.body.setAttribute('data-theme', 'dark') : document.body.removeAttribute('data-theme'), e.addEventListener('change', (t) => {
      e.checked ? (document.body.setAttribute('data-theme', 'dark'), localStorage.setItem('darkSwitch', 'dark')) : (document.body.removeAttribute('data-theme'), localStorage.removeItem('darkSwitch'));
    });
  }
}());
*/
